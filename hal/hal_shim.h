/* Compatibility alias — firmware source files in simulator/firmware/src
 * include "hal_shim.h". PySaacSim provides the same surface via hal.h. */
#ifndef PYSIM_HAL_SHIM_H
#define PYSIM_HAL_SHIM_H
#include "hal.h"
#endif
